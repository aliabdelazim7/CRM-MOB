        function loadProductSupplierOptions() {
            const select = document.getElementById('productSupplier');
            select.innerHTML = '<option value="">-- اختر المورد --</option>';
            suppliers.forEach(supplier => {
                select.innerHTML += `<option value="${supplier}">${supplier}</option>`;
            });
        }

        // Auto-fill supplier address when selecting supplier
        function loadSupplierAddress() {
            const supplierName = document.getElementById('purchaseSupplier').value;
            const addressField = document.getElementById('purchaseSupplierAddress');
            
            if (supplierName) {
                // Find supplier in the suppliers array
